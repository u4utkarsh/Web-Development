function whosPaying(names) {
    
    /******Don't change the code above*******/
        
        //Write your code here.
        var no = names.length;
        var index = Math.floor((Math.random()*no));
        return ( names[index] + " is going to buy lunch today!");
        
        
        
    
    
    /******Don't change the code below*******/    
    }